import 'dart:io';
import 'package:isar/isar.dart';
import 'lib/models/isar_schemas.dart';
import 'package:path_provider/path_provider.dart';

void main() async {
  // This is a scratch script to clear registration for testing
  print('🧹 Wiping local registration for testing...');
  // Note: This script is for illustration, the actual path depends on the platform.
  // We will instead implement a 'Clear Registration' button in the UI.
}
